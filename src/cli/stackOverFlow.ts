import { NS } from "@ns";
import { Autocomplete } from "/../CustomDefinitions";

export function autocomplete(ns: NS): Autocomplete {
    return[];
}
export async function main(ns: NS): Promise<void> {
    ns.tprint("Hello Remote API!");
}

